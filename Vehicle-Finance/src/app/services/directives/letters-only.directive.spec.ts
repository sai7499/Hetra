import { LettersOnlyDirective } from './letters-only.directive';

describe('LettersOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new LettersOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
