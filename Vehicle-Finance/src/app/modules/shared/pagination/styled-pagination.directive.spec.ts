import { StyledPaginationDirective } from './styled-pagination.directive';

describe('StyledPaginationDirective', () => {
  it('should create an instance', () => {
    const directive = new StyledPaginationDirective();
    expect(directive).toBeTruthy();
  });
});
